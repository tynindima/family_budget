import AppRouter from 'components/AppRouter';
import React, { FC } from 'react';
import './App.css';

const App: FC = () => (
  <div className="App">
    <AppRouter />
  </div>
);

export default App;
